# OVO 首次安装指引

面向「从零到第一次打开 OVO」的读者。更完整的顺序与可选环境见 **[docs/安装总结.md](docs/安装总结.md)**；图文教程见 **[docs/00-详细安装与使用教程.md](docs/00-详细安装与使用教程.md)**。

## 最短路径

1. 安装 **Python 3.11 或 3.12**，安装时勾选 **Add python.exe to PATH**。
2. 在项目根目录双击 **检查安装.bat**，确认 [1]、[2] 通过。
3. 双击 **启动.bat**，选 **1** 打开 **OVO 桌面窗口**（需已安装 pywebview，见 **检查安装.bat** 末尾提示）。

若需云模型（通义、DeepSeek 等），再安装 **Docker Desktop**，在 **启动.bat** 中选 **2** 启动 LiteLLM；API Key 与 LiteLLM 配置见 **docs/05-API-Key-配置说明.md** 与 **litellm/config.yaml**（占位符需替换为你的 Key）。

打包成单文件：在项目目录运行 **build_exe.bat**，得到 **dist\OVO.exe**（与 **litellm** 文件夹放在同一目录即可配合 Docker 使用）。
